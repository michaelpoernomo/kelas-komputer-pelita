export const apiPath = {
  tokenValidation: '/token/validation',
}

export const appPath = {
  login: '/login',
  murid: {
    list: '/murid/list',
  },
}

const path = {
  root: '/',
  home: '/home',
}

export default path
